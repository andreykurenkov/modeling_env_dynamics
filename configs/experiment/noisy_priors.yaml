# @package _global_
defaults:
  - /scene_gen: large_scenes
  - /agents: all
  - /data_gen: 5k
  - /task: predict_location
  - /model: gcn

run_name: noisy_priors
dataset: noisy_priors
scene_gen:
  priors_noise: 0.5
